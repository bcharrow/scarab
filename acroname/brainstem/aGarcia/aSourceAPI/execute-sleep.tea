#include <aCmd.tea>
#include <aCore.tea>
#include <aAPI.tea>

void main(unsigned int t)
{
  char i;
  if (t > 6553) {
    for (i = 0; i < 10; i++) {
      aCore_Sleep(t);
    }
  } else {
    aCore_Sleep(t * 10);
  }
  aAPI_StoreStatus(0);
}
