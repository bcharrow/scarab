
#include <aAPI.tea>
#include <aAPIStemDefs.tea>

void main(char x)
{
  if (x < 1) x = 1;
  if (x > 30) x = 30;
  aAPI_SetTxByteVar(0, apiSP03CMD);
  aAPI_SetTxByteVar(1, x);
  aAPI_SetTxLength(2);
  aAPI_BeginTx(apiSP03WRADDR);
}
