#include <aAPI.tea>
#include <aAPIGarcia.tea>

void main(char c)
{
  char x = 0;
  if (c) x = 1;
  aAPI_SetTxByte0(cmdRAW_INPUT);
  aAPI_SetTxByte1(aGARCIA_MOTO_RFLX_LED);
  aAPI_SetTxByteVar(2, x);
  aAPI_SetTxLength(3);
  aAPI_BeginTx(aGARCIA_MOTO_ADDR);
}
